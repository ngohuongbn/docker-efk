# TIMESTAMP {date time} LEVEL  API   [] MESSAGE

<source>
    @type tail
    path /var/log/mysqld.log
    pos_file /tmp/mysqld.log.pos
    <parse>
        @type grok
        grok_pattern %{MYSQLD}
        custom_pattern_path /etc/td-agent/conf/pattern
    </parse>
    tag mysql.daemon
</source>

<filter mysql.daemon>
    @type record_transformer
    enable_ruby
    <record>
        hostname "#{Socket.gethostname}"
        ip "#{(Socket.ip_address_list.detect do |intf| intf.ipv4_private? end).ip_address}"
        service ${tag_parts[0]}-${tag_parts[1]}
        # request ${request.gsub!("\u001b".encode('utf-8'), "");request.gsub(/\[0[0-2](;[0-9]{2})?m/, '')}
        # message ${message.gsub!("\u001b".encode('utf-8'), "");message.gsub(/\[0[0-2](;[0-9]{2})?m/, '')}
        # timestamp ${require 'date';timestamp.prepend(Date.today.to_s + "T");timestamp.concat("+05:30")}      # 2015-11-04T13:54:44+05:30
    </record>
</filter>

<match mysql.daemon>
    @type retag
    add_prefix controller.system 
</match>
