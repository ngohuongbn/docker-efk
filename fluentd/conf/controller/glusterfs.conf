<source>
  @type glusterfs_log
  path /var/log/glusterfs/cli.log
  pos_file /var/log/td-agent/glusterfs.log.pos
  tag glusterfs_log.glusterd
  format /^(?<message>.*)$/
</source>

<match glusterfs_log.**>
    @type retag
    add_prefix controller.system
</match>
